# Cloudflare Workers Assets 커스텀 헤더(/css/* 는 run_worker_first 밖이라 ASSETS 가 직접 서빙).
# 멀티 스킨 색 테마 CSS 는 콘텐츠 해시 파일명(skins-<hash>.css)이라 내용이 바뀌면 파일명도
# 바뀐다 → 영구 캐시해도 stale 위험이 없어 immutable 1년 캐시를 적용한다(빌드 산출물).
/css/skins-*
  Cache-Control: public, max-age=31536000, immutable

# Vite 청크는 파일명에 콘텐츠 해시가 들어가므로 브라우저가 재검증할 필요가 없다.
/dist/chunks/*
  Cache-Control: public, max-age=31536000, immutable
