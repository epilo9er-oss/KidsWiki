{
  "name": "kidswiki",
  "version": "1.0.0",
  "private": true,
  "packageManager": "npm@11.19.1",
  "allowScripts": {
    "esbuild": true,
    "sharp": true,
    "workerd": true
  },
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "predev": "node packages/wiki-shared/scripts/sync-render-css.mjs",
    "dev": "wrangler dev",
    "setup:local": "wrangler d1 execute DB --local --file=migrations/schema.sql && wrangler d1 migrations apply DB --local",
    "db:migrate:remote": "wrangler d1 migrations apply DB --remote",
    "dev:local": "wrangler dev --local",
    "deploy": "wrangler deploy",
    "build": "node packages/wiki-shared/scripts/sync-render-css.mjs && node scripts/build-skins-css.mjs && astro build && node scripts/copy-astro-pages.mjs && vite build",
    "build:astro": "node packages/wiki-shared/scripts/sync-render-css.mjs && node scripts/build-skins-css.mjs && astro build && node scripts/copy-astro-pages.mjs",
    "build:watch": "vite build --watch",
    "typecheck": "tsc --noEmit",
    "typecheck:client": "tsc --noEmit -p tsconfig.client.json",
    "test:rag": "node --test scripts/rag.test.mjs",
    "test:auth": "node --test scripts/auth-identities.test.mjs scripts/account-merge.test.mjs",
    "generate:rules": "node scripts/generate-rules.mjs",
    "preview:config": "node scripts/make-preview-config.mjs",
    "preview:init": "npm run test:preview-config && npm run preview:config && wrangler deploy -c wrangler.preview.toml",
    "preview:deploy": "npm run test:preview-config && npm run preview:config && wrangler versions upload -c wrangler.preview.toml",
    "test:preview-config": "node --test scripts/make-preview-config.test.mjs"
  },
  "dependencies": {
    "@block65/webcrypto-web-push": "^1.0.2",
    "hono": "^4.13.5"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260827.1",
    "@emnapi/core": "1.11.3",
    "@emnapi/runtime": "1.11.3",
    "@types/node": "^26.4.0",
    "astro": "^7.2.8",
    "smol-toml": "^1.8.0",
    "typescript": "^7.0.2",
    "vite": "^8.2.2",
    "wrangler": "^4.127.0"
  }
}
